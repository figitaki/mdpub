# Routing

Routes map to files in your content directory.

Examples:

- `/` → `content/index.md`
- `/foo` → `content/foo.md` (or `content/foo/index.md`)
- `/docs/routing` → `content/docs/routing.md`

Missing pages render a simple 404.
